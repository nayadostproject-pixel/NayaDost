NayaDost FINAL FIX

1) Render web service MUST use a paid plan with a persistent disk mounted at /var/data.
2) Set DB_DIR=/var/data and DB_PATH=/var/data/naya_dost.sqlite.
3) Set TELEGRAM_BOT_TOKEN to the SAME BotFather token used by @NayaDostMining_NYD_bot.
4) Set APP_URL=https://nayadost-3.onrender.com and APP_DOMAIN=nayadost-3.onrender.com.
5) For the two Telegram channel tasks, set EARN_CHANNEL_CHAT_ID and OFFICIAL_CHANNEL_CHAT_ID to the actual numeric chat IDs. The bot must be an admin/member able to call getChatMember.
6) Set BOT_API_KEY to one long random secret. Use the SAME value in the bot service.
7) Bot environment: BOT_TOKEN=<same BotFather token>, API_BASE_URL=https://nayadost-3.onrender.com, WEB_APP_URL=https://nayadost-3.onrender.com/, BOT_API_KEY=<same secret>.
8) The bot.py in this package no longer keeps a separate users.json. The backend SQLite database is the single source of truth for user balance/referrals.
9) Mining is server-enforced at 5000 taps/day and the UI reads today's count from the server.
10) Both Telegram channel tasks are forced to 200 NYD, including existing DB rows.
11) Withdrawal history, deposits, task claims, referrals, pending mining, balances, wallet verification and miner level/payment records are stored in SQLite.
12) TON miner payment uses a valid TON Connect `messages` transaction. USDT miner payment builds the jetton transfer payload server-side and then checks blockchain confirmation before unlocking the level.
13) Never put BOT_TOKEN, TONAPI keys or admin secrets into public/index.html.
